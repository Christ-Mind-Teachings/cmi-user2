{
  "name": "user2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "destroy": "claudia destroy",
    "create": "claudia create --name cmiUserAPI --region us-east-1 --timeout 10 --api-module src/index --policies policies",
    "create-old": "npm run transpile && claudia create --name cmiUser --region us-east-1 --timeout 10 --api-module bin/index --policies policies",
    "update": "claudia --no-optional-dependencies update",
    "update-old": "npm run transpile && claudia --no-optional-dependencies update",
    "transpile": "babel --presets es2015 src --out-dir bin",
    "precreate": "eslint src/**",
    "preupdate": "eslint src/**"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "claudia": "^5.12.0"
  },
  "dependencies": {
    "claudia-api-builder": "^4.1.2",
    "eslint": "^7.5.0",
    "mailgun-js": "^0.22.0",
    "md5": "^2.2.1"
  },
  "optionalDependencies": {
    "aws-sdk": "^2.715.0"
  }
}
